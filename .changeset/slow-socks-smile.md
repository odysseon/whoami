---
"@odysseon/whoami-core": minor
---

Ship the new feature-first core API for accounts, authentication, and receipts, add feature-layer tests, and include an Express example app that consumes the public core exports.

Mark the legacy `WhoamiService` facade and `WhoamiServiceDependencies` contracts as deprecated, with removal planned for `v4.0.0`.
